import { Time } from '@angular/common';

export class Shift {
    deptNumber: number;
    shiftNumber: number;
    startTime: string;
    endTime: string;
    timeOutMinutes: number;
    validFrom: boolean;
}
